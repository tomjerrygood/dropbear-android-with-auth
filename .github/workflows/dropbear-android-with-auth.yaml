name: Build Dropbear 2024.86 ARMv7 Android(API24)
on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  NDK_VERSION: r25c
  NDK_ROOT: ${{ github.workspace }}/android-ndk-r25c

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Download Android NDK r25c
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r25c-linux.zip
          unzip -q android-ndk-r25c-linux.zip

      - name: Download Dropbear 2024.86 source
        run: |
          wget -q https://mjt.dt.cc/code/dropbear/dropbear-2024.86.tar.bz2
          tar -xf dropbear-2024.86.tar.bz2

      - name: Prepare localoptions.h
        run: |
          cat > dropbear-2024.86/localoptions.h <<'EOF'
#define DROPBEAR_SVR_PASSWORD_AUTH 0
#define DROPBEAR_SVR_PUBKEY_AUTH 1
#define DROPBEAR_SFTPSERVER 1
#define DROPBEAR_USE_SSH_CONFIG 0
#define DROPBEAR_TCP_FORWARDING 1
#define DROPBEAR_UNIX_FORWARDING 0
EOF

      - name: Patch svr-auth.c add custom -A user:pass auth
        run: |
          cd dropbear-2024.86
          sed -i '/int check_password/a\
static char *custom_auth_user = NULL;\
static char *custom_auth_pass = NULL;\
\
void set_custom_auth(const char *s) {\
    if (!s) return;\
    char *sep = strchr(s, \':\');\
    if (sep) {\
        *sep = 0;\
        custom_auth_user = strdup(s);\
        custom_auth_pass = strdup(sep+1);\
    }\
}\
\
if (custom_auth_user && custom_auth_pass) {\
    if (strcmp(username, custom_auth_user)==0 && strcmp(password, custom_auth_pass)==0) {\
        return 1;\
    }\
}' src/svr-auth.c

      - name: Patch svr-tcpfwd.c / svr-runopts.c add -A option
        run: |
          cd dropbear-2024.86
          # add -A cmdline argument parsing
          sed -i '/static void usage/a\
"\t-A user:pass\tCustom static username/password for auth\n"' src/svr-runopts.c
          sed -i '/while ((c = getopt(argc, argv,/s/"$/,A"/' src/svr-runopts.c
          sed -i '/case \'A\':/a\
set_custom_auth(optarg);\
break;' src/svr-runopts.c
          sed -i '/extern void set_custom_auth/d' src/svr-runopts.c
          sed -i '1i extern void set_custom_auth(const char *s);' src/svr-runopts.c

      - name: Cross compile ARMv7 API24
        run: >
          set -e;
          set -x;
          cd dropbear-2024.86;
          export NDK=${{ env.NDK_ROOT }};
          export TOOLCHAIN=$NDK/toolchains/llvm/prebuilt/linux-x86_64;
          export TARGET=armv7a-linux-androideabi24;
          export AR=$TOOLCHAIN/bin/llvm-ar;
          export CC=$TOOLCHAIN/bin/${TARGET}-clang;
          export CXX=$TOOLCHAIN/bin/${TARGET}-clang++;
          export RANLIB=$TOOLCHAIN/bin/llvm-ranlib;
          export STRIP=$TOOLCHAIN/bin/llvm-strip;
          ./configure --host=armv7a-linux-androideabi
            --disable-zlib
            --enable-static
            --without-pam
            --disable-utmp
            --disable-utmpx
            --disable-wtmp
            --disable-wtmpx
            --disable-loginfunc;
          make -j1 dropbear dropbearkey;
          $STRIP dropbear dropbearkey;

      - name: Upload compiled binaries
        uses: actions/upload-artifact@v4
        with:
          name: dropbear-armv7-2024.86
          path: |
            dropbear-2024.86/dropbear
            dropbear-2024.86/dropbearkey
